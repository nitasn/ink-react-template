{
  "dependencies": {
    "ink": "^5.0.1",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.24.7",
    "@babel/core": "^7.24.7",
    "@babel/preset-react": "^7.24.7",
    "babel-plugin-transform-rename-import": "^2.3.0"
  },
  "scripts": {
    "build": "npx babel src --out-dir build --out-file-extension .mjs",
    "start": "npm run build && node build/index.mjs"
  }
}
