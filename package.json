{
  "dependencies": {
    "ink": "^5.0.1",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.24.7",
    "@babel/core": "^7.24.7",
    "@babel/preset-env": "^7.24.7",
    "@babel/preset-react": "^7.24.7",
    "babel-plugin-transform-rename-import": "^2.3.0",
    "nodemon": "^3.1.4"
  },
  "scripts": {
    "build": "npx babel src --out-dir build --out-file-extension .mjs",
    "dev": "npx nodemon --watch src --ext js,jsx --exec 'npm run build && node build/index.mjs'"
  }
}
